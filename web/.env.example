# Same Supabase project the worker uses, or a throwaway database for local UI work.
# Send to Unfetched clears article text — do not point this at production unless you mean to.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SITE_PASSWORD=
